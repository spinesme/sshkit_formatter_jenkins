require 'spec_helper'

describe SSHKitFormatterJenkins do
  it 'has a version number' do
    expect(SSHKitFormatterJenkins::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
